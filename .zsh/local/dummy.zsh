# Placeholder file to (a) ensure the directory exists and (b) prevent the "no files found" warning
#
# Files (*.zsh) in this directory are intended for settings / aliases / ... that are specific to this
# machine and should not be committed to git (or at least to the public repo).

